name: Build Debug APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch: # allows manual trigger from the GitHub UI

env:
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true  # suppress Node20 deprecation warnings

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      # 1. Check out source code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Set up JDK 17 (required by AGP 8.x)
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      # 3. Set up Android SDK
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # 4. Accept all Android SDK licenses (prevents build failures)
      - name: Accept Android SDK licenses
        run: yes | sdkmanager --licenses || true

      # 5. Install required SDK components
      - name: Install SDK components
        run: |
          sdkmanager "platforms;android-35" "build-tools;35.0.0" "platform-tools"

      # 6. Cache Gradle packages
      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', '**/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # 7. Make gradlew executable (chmod + explicit bash invocation avoids
      #    any remaining CRLF shebang issues on Linux runners)
      - name: Make gradlew executable
        run: chmod +x gradlew

      # 8. Build the debug APK — using "bash gradlew" instead of "./gradlew"
      #    so the OS shebang line is never parsed (bypasses CRLF issues)
      - name: Build Debug APK
        run: bash gradlew assembleDebug --stacktrace --no-daemon

      # 9. Upload the APK as a downloadable artifact
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: HabitTracker-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          retention-days: 14
